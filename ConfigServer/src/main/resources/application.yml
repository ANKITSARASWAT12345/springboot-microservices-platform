server:
  port: 8085

spring:
  application:
    name: CONFIGSERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ANKITSARASWAT12345/Microservices-config
          clone-on-start: true